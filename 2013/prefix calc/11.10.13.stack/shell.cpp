#include <iostream>
using namespace std;

template<typename IT>
void shell_sort(IT first, IT last) 
{
    for(int d = (last - first / 2); d != 0; d /= 2) 
    {
        cout << "d = " << d << endl;
        for(
    }
}

int main() 
{
    return 0;
}